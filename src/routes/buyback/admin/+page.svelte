<script lang="ts">
	// TODO

	// so on the admin page we want to be able to view all buyback requests
	// we'll need to see
	// the character name
	// the character id
	// the price of the buyback
	// the items in the buyback, prolly in a dropdown or something
	// the status of the buyback
	// the date of the buyback
	// the ID of the buyback

	// we'll also need to be able to approve or deny buybacks
	// possible buyback states are
	// pending
	// denied
	// completed
</script>

<div>admin view of buyback requests</div>
